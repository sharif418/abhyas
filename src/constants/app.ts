/** Single source of truth for the shipped app version (Settings + about). */
export const APP_VERSION = "১.১.০";

/** ISO date of this release (Bengali-facing builds note it in Settings). */
export const APP_RELEASE = "২০২৫-০৯";
